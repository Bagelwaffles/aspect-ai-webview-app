{
  "name": "ams-browser-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pair": "npm run build --silent && node dist/index.js pair",
    "start": "npm run build --silent && node dist/index.js run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "4.20.6",
    "typescript": "5.9.3"
  }
}
