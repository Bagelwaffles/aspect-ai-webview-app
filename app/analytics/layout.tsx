import type { Metadata } from "next"
export const metadata: Metadata = { title:"Analytics Agent Beta | Aspect Marketing Solutions", description:"Private customer workspace for deterministic CSV analysis.", robots:{index:false,follow:false} }
export default function AnalyticsLayout({children}:Readonly<{children:React.ReactNode}>){return children}
