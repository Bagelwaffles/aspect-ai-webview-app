# Operator session route

This route converts an already authenticated, exact-match AMS Google owner session into the existing signed internal operator cookie. It never accepts an admin password and never elevates a non-owner Google session.
